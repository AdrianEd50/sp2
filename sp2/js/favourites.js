/*import { retriveCartFav } from "./utils/favouriteCartFunction.js";

const favourites = retriveCartFav();

const conteiner = document.querySelector(".checkout-conteiner");

if (favourites.length === 0) {
  conteiner.innerHTML = "No items here!";
}

favourites.forEach((favourite) => {
  conteiner.innerHTML += `<div class="product">
                               <h4><${favourite.title}</h4> 
                               <a href="checkout.html" class="cart cta-cart">Add to cart</a> 
                            </div>`;
});*/
